# この置き場で作業するときのルール

作った人向けの覚え書きではなく、**AI（Claude）が毎回まもる決まりごと**。
2026-08-24、電波マップを作ったときの振り返りから作った。

## この置き場（keisanki）について

PWA の練習用。**1つの置き場に、アプリを何本でも入れる**方式。

| 場所 | アプリ | 住所 |
|---|---|---|
| `/`（ルート） | 計算機 | https://terakoshi.github.io/keisanki/ |
| `denpa/` | 電波マップ | https://terakoshi.github.io/keisanki/denpa/ |

置き場は GitHub Pages。`main` に入れると1分ほどで公開先が変わる。

---

# 毎回まもること

## 1. 「できないこと」は、作り始める前に正直に言う

ブラウザからは読めないものが多い（Wi-Fiの電波強度、電池残量、他アプリの情報、
細かい位置追跡など）。**まずそれを1〜2行で伝える。**

ただし**そこで止まらない**。次の順番で回り込んで、頼まれたものは最後まで作る。

1. 近いことを別の方法で測れないか（電波強度 → 通信の速さ・ゆらぎ・とぎれ）
2. それも当てにならない場面のために、**手で入れてもらう道**を必ず用意する
3. どうやって測っているか、何が限界かを、アプリの中とREADMEの両方に書く

「できません」で終わらせない。「代わりにこう測っています」まで作って渡す。

## 2. 新しいアプリは、フォルダを分けて足す

既存アプリのファイルは書き換えない。`新しい名前/` を作り、その中に
`index.html` ・ `manifest.webmanifest` ・ `sw.js` ・ アイコンを一式そろえる。
これでホーム画面に別アイコンとして並ぶ。

## 3. 裏方（`sw.js`）の掟 ← 事故が起きた場所

同じ置き場のアプリは、**保存版のしまい場所（キャッシュ）を共有している**。
油断すると互いを壊す。新しく `sw.js` を書くときも、既存のを流用するときも、必ずこの2つを守る。

- **自分のフォルダ直下以外の要求には手を出さない。**
  ふつうに書くと上のフォルダの `sw.js` が下のアプリまで受け持ってしまい、
  下のアプリを開いた瞬間、その中身が上のアプリの保存版として上書きされる。
- **保存版の片づけは、自分の名前で始まるものだけ。**
  「自分以外を全部消す」と書くと、隣のアプリの保存版を消してしまう
  （`keisanki-` は `keisanki-` だけ、`denpa-` は `denpa-` だけ消す）。

**既存アプリの `sw.js` は、新しいアプリを足す前に必ず読む。**
今回はこれを後回しにしたせいで、作り終えてから2件直すことになった。

`sw.js` を直したら、中の版数（`keisanki-v3` の数字）を1つ増やす。
測定などで「毎回ほんとうに取りに行きたい」ファイルは、保存版で答えないよう除外する。

## 4. 出す前に、本物のブラウザで動かして確かめる

「たぶん動く」で渡さない。Playwright で **iPhone の画面サイズ**で開き、
実際に押して確かめてから報告する。最低限これだけは通す。

- 主な操作をひととおり（作る・消す・元に戻す）
- **閉じて開き直しても内容が残るか**
- JSのエラーが出ていないか
- **スクリーンショットを撮って、目で見る**（今回はこれで色ぬりの粗さに気づいて直した）
- 既存アプリを壊していないか（今回なら計算機で 7×8=56 を確認）
- 電波がなくても開けるか、複数アプリの保存版が共存するか

環境のこつ:
- ブラウザは `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` を
  `executablePath` で指す（`playwright install` はしない）
- **`127.0.0.1` ではなく `localhost` で開く。**
  `127.0.0.1` だと裏方（Service Worker）が登録されず、確認にならない
- アイコンは Python の Pillow で作る（無ければ `pip install pillow`）

**テストの条件が本番と違うことは自覚する。** 今回、手元のサーバーで測ると
返事が3msで常に満点になった。これは「測れている」であって「正しい」ではない。

## 5. ぜんぶ日本語で、専門用語を使わずに書く

画面の文字、README、コミットメッセージ、**コードの中のコメント**まで。
作った人が半年後に読んで分かる言葉にする。

- ✕ キャッシュ / スコープ / RSSI / 補間　→　○ 保存版 / 受け持ち / 電波の強さ / にじませる
- 数式や仕組みには「なぜそうするか」を一行そえる

## 6. README を必ず書き足す

アプリを足したら、README に次を書く。**書き終わるまでが作業。**

- そのアプリの住所と、できること
- **どういう仕組みで動いているか**（特に、ごまかしている所・限界）
- 使うときのコツ
- 「分かったこと（次に作るときのメモ）」に、今回学んだことを1〜2行足す

## 7. git の手順

- 作業は指定されたブランチで行う
- コミットメッセージは日本語。**何を直したか＋なぜそうしたか**を書く
- `main` に入れるのは、頼まれたときだけ。入れたら公開先が変わることを伝える
- モデル名（Claude / Opus など）はコミットや公開ファイルに書かない

## 8. 確かめられなかったことは、確かめられなかったと言う

今回、公開先（github.io）にこの作業環境から出られず、実際に開けるかを確認できなかった。
**「たぶん大丈夫です」で流さず、何を確認して何を確認できていないかを分けて報告する。**

---

# 過去にやらかしたこと

| いつ | 何が起きたか | 次からどうするか |
|---|---|---|
| 2026-08-25 | iPhone に直した内容が反映されない | 画面本体は「まず最新を取りに行く」方式にする。`sw.js` の版数を上げる |
| 2026-08-24 | 上のフォルダの `sw.js` が下のアプリまで受け持ち、保存版を上書きしていた | ルール3。アプリを足す前に既存の `sw.js` を読む |
| 2026-08-24 | 2つのアプリが互いの保存版を消し合っていた | ルール3。片づけは自分の名前で始まるものだけ |
| 2026-08-24 | 色ぬりがブロック状に粗かった（コードだけ見ても気づけない） | ルール4。必ずスクリーンショットを撮って目で見る |
